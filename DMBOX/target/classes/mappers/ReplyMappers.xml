<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reply">
	<insert id="reply_insert" parameterType="com.dm.org.ReplyDTO"> 
		insert into reply ( 
			reply_idx, post_idx, content, writer, depth, parent
		) values ( 
			#{reply_idx}, #{post_idx}, #{content}, #{writer}, #{depth}, #{reply_idx}
		) 
	</insert>
	<insert id="rereply_insert" parameterType="com.dm.org.ReplyDTO"> 
		insert into reply ( 
			reply_idx, post_idx, content, writer, depth, parent
		) values ( 
			#{reply_idx}, #{post_idx}, #{content}, #{writer}, #{depth}, #{parent}
		) 
	</insert>
	<select id="boardcount" resultType="com.dm.org.ReplyDTO" parameterType="int">
		select * from reply
		where post_idx = #{idx}
		ORDER BY parent, depth
	</select>
</mapper>